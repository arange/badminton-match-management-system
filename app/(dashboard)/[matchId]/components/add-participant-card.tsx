import { Button } from '@/components/ui/button';

export default function AddParticipantCard() {
  return (
    <div className="flex flex-col justify-center items-start gap-2 border rounded border-black w-fit p-3 min-w-40">
      <Button>Add participant</Button>
    </div>
  );
}
